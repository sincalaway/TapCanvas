{
  "name": "tapcanvas-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.8.1",
  "scripts": {
    "tap": "node packages/cli/bin/tapcanvas.js",
    "dev:web": "pnpm --filter @tapcanvas/web dev",
    "dev:api": "pnpm --filter cloudflare-workers-openapi dev",
    "deploy": "bash ./scripts/deploy.sh --all",
    "deploy:api": "bash ./scripts/deploy.sh --api",
    "deploy:ai-backend": "bash ./scripts/deploy.sh --ai-backend",
    "build:app": "pnpm --filter webcut run build:app",
    "build": "pnpm --filter @tapcanvas/web build",
    "build:api": "pnpm --filter cloudflare-workers-openapi deploy",
    "test": "pnpm -w -r run test",
    "deploy:web:cf": "pnpm --filter @tapcanvas/web build && npx wrangler --config wrangler.toml deploy --keep-vars",
    "deploy:webcut": "pnpm --filter webcut run build:app && npx wrangler --config apps/webcut-main/wrangler.toml deploy --keep-vars"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.10",
    "wrangler": "^3.90.0",
    "zod": "^3.25.76"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "workerd"
    ]
  }
}
