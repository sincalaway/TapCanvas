# TapCanvas 根目录 .env 示例
#
# 注意：
# - `apps/web`（Vite）不会读取仓库根目录的 `.env`，它读取 `apps/web/.env*`
# - `apps/hono-api`（Cloudflare Workers）通过 `wrangler*.jsonc/.toml` 注入变量，不读取根 `.env`
# - 因此：根目录 `.env` 仅建议用于「本地工具/脚本」或你自己的开发习惯；如不需要可忽略本文件
#
# 真实的运行时配置请参考：
# - `apps/web/.env.example`
# - `apps/hono-api/wrangler.example.jsonc`