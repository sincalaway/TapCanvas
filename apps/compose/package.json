{
  "name": "@tapcanvas/compose",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start": "echo 'Compose worker placeholder. Add renderer service here.'",
    "build": "echo 'Compose build placeholder'",
    "test": "echo 'No tests yet' && exit 0"
  }
}

